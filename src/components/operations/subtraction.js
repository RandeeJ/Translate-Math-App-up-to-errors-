import React, { Component } from 'react';

export default class Subtraction extends Component {
    render() {
        return (
            <div>
                subtraction
            </div>
        );
    }
}